# Android 专用依赖文件 - 锁定版本确保兼容性
# 文件名(File): requirements-android.txt
# 版本(Version): v1.0.0
# 创建日期(Created): 2025/7/25
# 简介(Description): Android打包专用依赖，解决Python 3.10+兼容性问题

# 核心框架 - 锁定版本
kivy>=2.3.0,<3.0.0
kivymd==1.1.1

# 音频视频处理
plyer>=2.1.0,<3.0.0
ffpyplayer>=4.5.0,<5.0.0

# 网络通信
websocket-client>=1.6.0,<2.0.0
aiohttp>=3.8.0,<4.0.0

# Android 兼容性关键依赖
pyjnius>=1.5.0,<2.0.0

# 构建工具 - 锁定版本避免兼容性问题
buildozer>=1.5.0,<2.0.0
cython>=0.29.36,<0.30.0

# 其他必要依赖
requests>=2.28.0,<3.0.0
urllib3>=1.26.0,<2.0.0 